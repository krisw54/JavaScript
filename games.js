    /* Open when someone clicks on the span element */
function openNavNG() {
    document.getElementById("numGame").style.width = "100%";
}

/* Close when someone clicks on the "x" symbol inside the overlay */
function closeNavNG() {
    document.getElementById("numGame").style.width = "0%";
}

    /* Open when someone clicks on the span element */
function openNavSEG() {
    document.getElementById("SeGame").style.width = "100%";
}

/* Close when someone clicks on the "x" symbol inside the overlay */
function closeNavSEG() {
    document.getElementById("SeGame").style.width = "0%";
}